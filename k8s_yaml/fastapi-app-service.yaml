apiVersion: v1
kind: Service
metadata:
  name: fastapi-app-service
  namespace: fastapi-production
spec:
  selector:
    app: fastapi
  type: ClusterIP
  ports:
  - name: http-fastapi
    protocol: TCP
    port: 8080
    targetPort: 8080
